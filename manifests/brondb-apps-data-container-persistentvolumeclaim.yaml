apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: brondb-apps-data-container
  name: brondb-apps-data-container
  namespace: bron
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
