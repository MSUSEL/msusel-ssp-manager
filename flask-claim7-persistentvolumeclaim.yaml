apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: flask-claim7
  name: flask-claim7
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
