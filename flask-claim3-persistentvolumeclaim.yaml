apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: flask-claim3
  name: flask-claim3
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
