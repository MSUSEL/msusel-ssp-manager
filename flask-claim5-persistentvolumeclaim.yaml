apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: flask-claim5
  name: flask-claim5
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
